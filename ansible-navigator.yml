# # cspell:ignore cmdline, workdir
---
ansible-navigator:
  ansible:
    inventory:
      entries:
        - hosts.yml
  execution-environment:
     enabled: false
  playbook-artifact:
    enable: false
  logging:
    level: error
    append: false
    file: ansible-navigator.log
