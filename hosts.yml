---
all:
  hosts:
    nuc:
      ansible_host: "{{ domain_name }}"
    recipe_vps:
      ansible_host: "{{ domain_name }}"
    client_vps:
      ansible_host: "{{ domain_name }}"

  children:
    servers:
      hosts:
        nuc:
        recipe_vps:
    clients:
      hosts:
        client_vps:
    hypervisors:
      hosts:
        nuc:
    boxes:
      hosts:
        nuc:
    container_hosts:
      hosts:
        nuc:
        recipe_vps:
