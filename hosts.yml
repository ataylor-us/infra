---
all:
  hosts:
    nuc:
      ansible_host: "{{ domain_name }}"
    recipe_vps:
      ansible_host: "{{ domain_name }}"
    client_vps:
      ansible_host: "{{ domain_name }}"
    workstation:
      ansible_host: "{{ domain_name }}"
    office_pc:
      ansible_host: "{{ domain_name }}"
    cluster0:
      ansible_host: "{{ cluster0_ip }}"
    cluster1:
      ansible_host: "{{ cluster1_ip }}"
    cluster2:
      ansible_host: "{{ cluster2_ip }}"

  children:
    linux_hosts:
      hosts:
        nuc:
        recipe_vps:
        client_vps:
        workstation:
    servers:
      hosts:
        nuc:
        recipe_vps:
        workstation:
    guests:
      hosts:
        client_vps:
        recipe_vps:
    clients:
      hosts:
        client_vps:
        workstation:
    hypervisors:
      hosts:
        nuc:
        workstation:
    boxes:
      hosts:
        nuc:
        workstation:
    exit_nodes:
      hosts:
        nuc:
        workstation:
    interactive_boxes:
      hosts:
        workstation:
    container_hosts:
      hosts:
        nuc:
        recipe_vps:
        workstation:
    labs:
      hosts:
        cluster0:
        cluster1:
        cluster2:
