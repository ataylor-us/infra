---
all:
  hosts:
    nuc:
      ansible_host: "{{ domain_name }}"
    recipe_vps:
      ansible_host: "{{ domain_name }}"
    client_vps:
      ansible_host: "{{ domain_name }}"
    workstation:
      ansible_host: "{{ domain_name }}"

  children:
    servers:
      hosts:
        nuc:
        recipe_vps:
        workstation:
    guests:
      hosts:
        client_vps:
        recipe_vps:
    clients:
      hosts:
        client_vps:
        workstation:
    hypervisors:
      hosts:
        nuc:
        workstation:
    boxes:
      hosts:
        nuc:
        workstation:
    interactive_boxes:
      hosts:
        workstation:
    container_hosts:
      hosts:
        nuc:
        recipe_vps:
        workstation:
    homedir_host:
      hosts:
        workstation:
    homedir_clients:
      hosts:
        nuc:
        client_vps:
